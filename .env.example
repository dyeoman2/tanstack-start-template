# Production Environment Variables Example
# Copy this file to .env.local for local development
# For production deployment, set these in your hosting platform (Netlify, Vercel, etc.)

# ==========================================
# REQUIRED SECRETS (Generate Securely)
# ==========================================

# Signs JWTs for authentication and protects internal rate limiting operations
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-32-byte-base64-secret-here

# ==========================================
# REQUIRED PRODUCTION SETTINGS
# ==========================================

# Your Convex deployment URL (from Convex Dashboard)
# Format: https://your-project-name.convex.cloud
VITE_CONVEX_URL=https://your-project-name.convex.cloud

# Your Convex site URL for client connections
# Replace .cloud with .site in VITE_CONVEX_URL
VITE_CONVEX_SITE_URL=https://your-project-name.convex.site

# Application name for email templates and branding
APP_NAME=Hackathon

# ==========================================
# OPTIONAL SERVICES
# ==========================================

# Email service (Resend) - required for password reset and notifications
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxx

# Verified sender email (must be verified in Resend)
RESEND_EMAIL_SENDER=onboarding@resend.dev

# Pricing and billing (Autumn) - optional
# Get your secret key from: https://app.useautumn.com/settings
AUTUMN_SECRET_KEY=am_sk_xxxxxxxxxxxxxxxxxxxx

# ==========================================
# STORAGE (AWS S3 for Production)
# ==========================================

# AWS S3 configuration (replace with your actual values)
S3_ENDPOINT=https://s3.us-east-1.amazonaws.com
S3_REGION=us-east-1
S3_ACCESS_KEY_ID=your-aws-access-key-id
S3_SECRET_ACCESS_KEY=your-aws-secret-access-key
S3_BUCKET_NAME=your-production-bucket-name
S3_FORCE_PATH_STYLE=false
S3_PUBLIC_URL=https://your-cdn-url.com

# ==========================================
# DEVELOPMENT OVERRIDES (Local Only)
# ==========================================

# These are automatically set for local development by setup scripts
# NODE_ENV=development

# Convex deployment name (auto-detected by setup scripts)
# CONVEX_DEPLOYMENT=dev:your-project-name

# Local storage (MinIO) - auto-configured for development
# S3_ENDPOINT=http://localhost:9000
# S3_REGION=us-west-1
# S3_ACCESS_KEY_ID=minioadmin
# S3_SECRET_ACCESS_KEY=minioadmin
# S3_BUCKET_NAME=application-documents
# S3_FORCE_PATH_STYLE=true
# S3_PUBLIC_URL=http://localhost:9000
