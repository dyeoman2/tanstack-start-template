/**
 * Defaults used by `setup:env`, `setup:convex`, and `setup:prod`.
 * Keep aligned with fallbacks in `convex/betterAuth/sharedOptions.ts` when changing branding.
 */
export const DEFAULT_APP_NAME = 'TanStack Start Template';

export const DEFAULT_PROD_RESEND_SENDER = 'onboarding@resend.dev';
